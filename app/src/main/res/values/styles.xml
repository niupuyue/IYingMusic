<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <style name="AppTheme.First" parent="AppTheme">
        <!--<item name="android:windowFullscreen">true</item>-->
        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

    <!-- 透明渐变短动画 -->
    <style name="FadeAnimationShort" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/anim_pop_opaque</item>
        <item name="android:windowExitAnimation">@anim/anim_pop_alpha</item>
    </style>

    <!-- 音乐加入我的喜欢 -->
    <style name="style_music_item_favorite" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:button">@drawable/music_item_favorite_style</item>
    </style>

</resources>
